# AGENTS

This repo defines the MVP agents for WEXARE (PlannerAgent, OrchestratorAgent, QAAgent) and their Tool Registry.

- Agents are backend orchestrators.
- They never mutate the scope of a funded quest.
- Evidence is mandatory for submissions, disputes, and votes.

See `backend/app/agents/` and `ARCHITECTURE.md`.
